<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activity.GuiActivity">

    <android.support.v4.view.ViewPager
        android:id="@+id/guide_viewpage"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <Button
        android:id="@+id/guide_button"
        android:layout_width="@dimen/xa468"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="@dimen/xa140"
        android:padding="@dimen/xa23"
        android:text="@string/m1立即体验"
        android:background="@null"
        android:textColor="@color/green_1"
        android:textSize="@dimen/xa35"
        android:visibility="gone" />

</RelativeLayout>
